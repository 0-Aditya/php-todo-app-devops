pipeline {
    agent any

    stages {
        stage('Clone Repo') {
            steps {
                checkout scm
                echo "Repository cloned successfully!"
            }
        }
    }
}
